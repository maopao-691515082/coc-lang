public native void assert(bool cond);

public native final class String
{
    //public String format(...); //内建特殊方法，专用于常量格式字符串的格式化

    public long size();

    public char __op_item_get(long idx);
    public int __op_cmp(String other);
    public String __op_add(String s);
    public String __op_iadd(String s);
}
