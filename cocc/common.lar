import time;

bool using_verbose_mode = false;

void verbose_log(String msg)
{
    if (using_verbose_mode)
    {
        println("cocc: [%s] %s".(time.format_time("15:04:05", time.time()), msg));
    }
}

class ExitSignal
{
    EmptyException e usemethod;
}

void exit(String msg)
{
    err_println("错误：%s".(msg));
    err_println("");
    throw(new ExitSignal{});
}
